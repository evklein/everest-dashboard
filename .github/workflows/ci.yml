name: CI
on:
  pull_request:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-16.04
    steps:
      - uses: actions/checkout@v1
      - name: Build
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: '6.0.x'
      - run: |
          echo "Everest Build"
          ls -a
          cd Everest/
          dotnet build
