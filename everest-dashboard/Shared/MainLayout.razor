@inherits LayoutComponentBase

<PageTitle>Everest</PageTitle>

<MudThemeProvider/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout>
    <MudAppBar Color="Color.Primary" Fixed="false">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick=@toggleDrawer/>
        <MudSpacer />
        <MudText Typo="Typo.h5" Class="ml-3"><b>Ev</b>erest</MudText>
    </MudAppBar>
    <MudDrawer @bind-Open=@_open Elevation="1">
        <MudDrawerHeader>
            <MudText Typo=@Typo.h6>Pages</MudText>
        </MudDrawerHeader>
        <MudNavMenu>
            <MudNavLink Href="/Notes" Icon=@Icons.Material.Filled.Feed>Notes</MudNavLink>
        </MudNavMenu>
    </MudDrawer>
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>

@code
{
    private bool _open = false;

    private void toggleDrawer()
    {
        _open = !_open;
    }
}

